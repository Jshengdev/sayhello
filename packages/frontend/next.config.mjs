/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* no dark Next "N" disc floating in the paper world during the judge demo */
  devIndicators: false,
};

export default nextConfig;
