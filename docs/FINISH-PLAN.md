# FINISH-PLAN — ~5 prompts to a winning demo by 4:00 (S1 stub is DONE; we're upgrading, not surviving)

> You already have a working end-to-end demo on stub. The job now: make it REAL, BEAUTIFUL, SPONSOR-VISIBLE,
> and bulletproof. "Works no matter what" = go live on one lead but CACHE the real run so the demo never breaks.

## Priority order (if you only get 3 done, do 1-2-3)
1. **GO LIVE + CACHE** — one real gtm lead end-to-end, a GENUINE catch, cached as replayable fallback.
2. **VISUAL POLISH** — the catch beat, the spiral climb, the gate-stamp gorgeous per DESIGN.md.
3. **SPONSOR SURFACES** — Langfuse trace, ClickHouse trajectory panel, OpenUI/C1 story slides, all on screen + captioned.
4. **DEPLOY + LENS SWAP** — Render live; realestate + marketing on cached leads ("it generalizes").
5. **BULLETPROOF** — full dry-run, fix demo-path bugs, REPLAY_MODE fallback verified, record the video.

## The "never breaks" rule
Three modes, in order of preference at demo time: LIVE (real APIs) → REPLAY (cached real run, same timing) →
STUB (canned). Wire REPLAY in prompt 1. At 4:30 you run LIVE; if wifi/API flakes, one keypress → REPLAY of
the SAME real lead. STUB is the floor. The demo cannot fail.

## You provide
- ONE real SF SaaS company URL for the live gtm demo (ideally one whose site tempts an ungrounded claim).
- The 3 free keys in .env (Langfuse, Thesys, Render) if not already there.
